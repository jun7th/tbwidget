{
  "name": "tbwidget",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:kui": "node scripts/sync-kui-vendor.mjs",
    "check:plugins": "node scripts/check-bundled-plugins.mjs",
    "postinstall": "npm run sync:kui",
    "predev": "npm run check:plugins && npm run sync:kui",
    "dev": "vite",
    "prebuild": "npm run check:plugins && npm run sync:kui",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@fontsource/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@fontsource/manrope": "^5.3.0",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "dayjs": "^1.11.13",
    "vue": "^3.5.13",
    "kui-vue": "5.8.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
