# 当前重构结果

- UI 底座切换为 `kui-vue`，默认 dark theme。
- 移除 `tb-*` Web Components 主链路。
- 插件宿主不再提供 GPT、系统监控、任务栏透明等业务/专用接口。
- 插件基础能力统一为 HTTP、文件读取、隔离存储和插件 WASM。
- GPT Usage 示例的业务逻辑已迁回插件自身。
- Popup 改为按真实内容持续测量并自动调整原生窗口尺寸，不再固定 220–480px。
